package ${packageName};

${imports}
import com.desmond.ecommerce.base.model.impl.BaseModelImpl;

public class ${modelImpl} extends BaseModelImpl implements ${model}{
	${privateFields}
	${setterAndGetter}
	
	public ${model} mock${model}Impl() {
		${model} ${modelVariable} = new ${model}Impl();
		${mockSetValue}
		
		return ${modelVariable};
	}
}
